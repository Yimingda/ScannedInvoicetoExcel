# 依赖下限放宽，兼容 Streamlit Cloud 的 Python（3.11~3.13）自动选版。
# 不要把版本钉到本机 Python 3.14 才有的最新 wheel，否则云端 pip 找不到会失败。
rapidocr-onnxruntime>=1.2.3
# onnxruntime 由 rapidocr-onnxruntime 自动拉取兼容版本，故不显式钉版
pymupdf>=1.23
pdfplumber>=0.11
openpyxl>=3.1
pillow>=9.5
# 注：rapidocr-onnxruntime 硬依赖完整版 opencv-python，会自动装上；
# 完整 opencv 运行需 libGL.so.1 与 glib，见 packages.txt（libgl1 / libglib2.0-0t64）
numpy>=1.23
pyyaml>=6.0
streamlit>=1.40
pandas>=1.5,<3
